-The data-* : attribute is just a way to attach extra, custom information to an HTML element — information that is not normally part of the element’s attributes like id, class, src, etc.
-tagName always returns Upppercase name
-for getting all child elemenets first access parent element then convert it to array and apply the evenet through every element
-children which is a property of document return a html collection it is an array like array like doucment -> childrent -> html [htmlcollection], document.body.children -> all tags [htmlcollection]  
-
so always use remove instead of remove child because it works regardless of which is children or element remove() modern , removechild() old
-------------
📝 Summary

✅ parent.replaceChild(new, old) → use when you already have parent reference.

✅ old.replaceWith(new) → simpler, directly on the element to be replaced.

❌ parent.replaceWith(...) → replaces the entire parent element itself.